Changes made on resubmission 

- Updated the Engine class constructor by arranging the parameters in the order so that variables are initialized with the right value. 
- Update the addPassenger() function in the constructor so that it returns false when a passenger who has arleady boarded is added. 
- Removed redundant attributes maxCapacity and seatsRemaining from the Train class and instead calculated the total from the Car classes values
- Added JavaDoc comments where they were missing